spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
      name: ureka
  security:
    user:
      name: config_user
      password: config_password

## not work
server:
  port: 8888
