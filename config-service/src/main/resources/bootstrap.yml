spring:
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: gitlab/repo/uri
          username: gitlab_id
          password: gitlab_password
      name: ureka
  security:
    user:
      name: config_user
      password: config_password

## not work
server:
  port: 8888
