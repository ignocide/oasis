server:
  port: ${PORT:8085}

spring:
  application:
    name: task-service
  cloud:
    config:
      uri: http://localhost:8888
      username: config_user
      password: config_password
      name: task-service,ureka
  jpa:
    hibernate:
      ddl-auto: update
# log
#logging:
#  config: classpath:logback-spring.xml
